<UserControl x:Class="PointOfSale.CustomizeCowpokeChili"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PointOfSale"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <CheckBox IsChecked="{Binding Path=Cheese}" Content="Cheese" Margin="10,10,10,10" Background="White" FontWeight="Normal" FontSize="20"/>
            <CheckBox IsChecked="{Binding Path=TortillaStrips}" Content="Tortilla Strips" Margin="10,10,10,10" FontSize="20"/>
        </StackPanel>
        <StackPanel Grid.Column="1">
            <CheckBox IsChecked="{Binding Path=SourCream}" Content="Sour Cream" Margin="10,10,10,10" FontSize="20"/>
            <CheckBox IsChecked="{Binding Path=GreenOnions}" Content="Green Onions" Margin="10,10,10,10" FontSize="20">
                <CheckBox.Background>
                    <ImageBrush/>
                </CheckBox.Background>
            </CheckBox>
        </StackPanel>
    </Grid>
</UserControl>
